<%- include('partials/header') -%>
<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= room.title %></h2>
      <img class="img-fluid" src="<%= room.image%>" />
      <div class="row justify-content-between">
        <form
          class="col-1"
          action="/room/likeRoom/<%= room.id %>?_method=PUT"
          method="POST"
        >
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-3">Likes: <%= room.likes %></h3>
        <%if(room.user == user.id){ %>
        <form
          action="/room/deleteRoom/<%= room.id %>?_method=DELETE"
          method="POST"
          class="col-3"
        >
          <button class="btn btn-primary fa fa-trash" type="submit"></button>
        </form>
        <%}%>
      </div>
    </div>
    <div class="col-3 mt-5">
      <p><%= room.caption %></p>
    </div>
    <div class="col-6 mt-5">
      <a class="btn btn-primary" href="/profile">Return to Profile</a>
      <a class="btn btn-primary" href="/feed">Return to Feed</a>
      <a class="btn btn-primary" href="/room">Return to Room</a>
    </div>
  </div>
</div>

<%- include('partials/footer') -%>
